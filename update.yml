---
    - name: install apache
      become: true
      ansible_builtin.apt:
        become_user: parallels
        name: apache2 
        update_cache: yes 
        state: latest

    - name: install ansible
      apt: name=ansible update_cache=yes state=latest

